{
  "name": "agent-deals",
  "version": "0.1.0",
  "private": true,
  "description": "国内 Agent / Coding Plan 优惠活动汇总站（静态站 + 云端定时爬取 + 变更推送）",
  "scripts": {
    "dev": "next dev",
    "prebuild": "node scripts/gen-rss.mjs",
    "build": "next build",
    "crawl": "node scripts/crawl.mjs",
    "crawl:baseline": "node scripts/crawl.mjs --baseline",
    "verify:ui": "node scripts/verify-ui.mjs",
    "verify:expiry": "node scripts/verify-expiry.mjs"
  },
  "dependencies": {
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
